[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && pnpm install && cd apps/admin-dashboard && pnpm build"

[deploy]
startCommand = "cd apps/admin-dashboard && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
